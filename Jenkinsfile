pipeline {
    agent any
    stages {
        stage('Build') {
            steps {

            }
        }

        stage('Test') {
            steps {
                sh 'npm test'
            }
        }

        
    }
}